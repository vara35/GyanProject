import tw from 'twin.macro'
import styled from 'styled-components'

export const ImageAndCommentsContainer = styled.div`
   padding-top: 40px;
   ${tw`flex flex-row items-center`}
`
export const CommentsText = styled.p`
   ${tw`font-sans h-6`}
   width: 89px;
   margin: 0 0 0 4px;
   font-size: 15px;
   line-height: 1.5;
   color: #0b69ff;
`
